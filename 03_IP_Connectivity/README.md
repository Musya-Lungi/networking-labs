Domain 3 Objectives
✓ Interpret all components of a routing table: route sources, AD, metric, next-hop, exit interface, timestamps
✓ Determine how a router makes a forwarding decision by default: longest prefix match, AD, metric
✓ Configure and verify IPv4 static routing: standard, default, summary, floating static routes
✓ Configure and verify IPv6 static routing: static and default routes
✓ Configure and verify single-area OSPFv2: router ID, network statements, passive interfaces, DR/BDR election
✓ Describe FHRP (First Hop Redundancy Protocols): HSRP, VRRP, GLBP — concept and purpose